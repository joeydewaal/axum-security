[package]
name = "basic-auth"
version = "0.1.0"
edition = "2024"

[dependencies]
axum-security = { path = "../../axum-security", features = ["basic-auth"] }
tokio = { version = "1.49", features = ["full"] }
axum = { version = "0.8.8" }
